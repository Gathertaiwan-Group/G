[build]
builder = "NIXPACKS"
buildCommand = "npm install --workspaces && npm run build -w @realreal/control-db && npm run build -w workers"

[deploy]
startCommand = "node apps/workers/dist/index.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
