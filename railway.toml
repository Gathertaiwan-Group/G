[build]
builder = "NIXPACKS"
buildCommand = "npm install --workspaces && npx turbo build --filter=workers"

[deploy]
startCommand = "node apps/workers/dist/index.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
